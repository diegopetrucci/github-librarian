---
description: Spawn the github-librarian subagent to investigate something on GitHub
argument-hint: <query>
---

Invoke the `github-librarian` subagent via the Task tool with this query:

$ARGUMENTS

Pass the query verbatim — do not rephrase or expand it. Surface the agent's markdown response to the user without rewriting.
